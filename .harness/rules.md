# Repository Rules

- Inspect before editing and make the smallest reasonable change.
- Preserve uncommitted work; never commit, push, reset, or discard changes.
- Only configured verification commands are run automatically.